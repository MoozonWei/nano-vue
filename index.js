/* eslint-disable no-console */
import { Ref, createApp, diff, effectWatch, h, mountElement, reactive, ref } from './core/index.js'

console.log('nano vue module loaded')

export {
  Ref,
  createApp,
  diff,
  effectWatch,
  h,
  mountElement,
  reactive,
  ref,
}
